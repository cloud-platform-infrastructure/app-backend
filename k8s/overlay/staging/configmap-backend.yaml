apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: backend-staging
data:
  REQUIRE_SLACK: "true"
  AWS_REGION: "us-east-1"
  DYNAMODB_TABLE: "slack-enterprise-study-staging-use1-logs"